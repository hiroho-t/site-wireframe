---
name: site-wireframe
description: Webサイトのセクション単位のワイヤーフレームをHTMLで作るときに使う。下層ページタイトル・MV・1カラム・2カラム・3カラム・4カラム・5カラム・お知らせ・フッターの9カテゴリ167型を1440×800のアートボードで持つ。「サイトのワイヤーを作って」「トップページのワイヤーを1枚で」「MVのレイアウト案がほしい」「お知らせ一覧の構成を」「フッターのワイヤーを」などの依頼で必ず使うこと。スクリーンショットを渡されてレイアウトを実測・トレースする場面でも使う。明示的に「スキル」と言われなくても、コーポレートサイト・サービスサイトのセクション構成をHTMLで組む場面では積極的に発火させる。
---

# サイトワイヤーフレーム テンプレート集

Webサイトのセクション型を収録している。実在のサイトのスクリーンショット、またはURLからレイアウトだけを採寸・再現し、テキストはすべてダミー化してある。**新規にゼロから組まず、まずこの中から近い型を選んで流用する。**

## 使い方

0. **型を見る前に、入れる情報を数える。**
   セクションごとに「見出し何行／リード何行／**項目何件**／導線いくつ／画像があるか」を書き出す。
   そのうえで、その数を受けられる型だけを候補にする（**件数から引くなら [SELECT.md](SELECT.md) の「項目の件数から引く」**）。
   カタログの各行末に「**◯件**」と入れてあるのが、その型が並べられる項目数。
   **型に入らないからといって、原稿の文を削らない。型のほうを選び直す。**

1. 下のカタログから、目的とテキスト量に合う型を選ぶ（**選び分けに迷ったら [SELECT.md](SELECT.md)**。骨格別インデックス・カテゴリ別のデフォルト・似た型の使い分けをまとめてある）
2. 該当ファイル（例 `mv/mv-001.html`）を読む
3. コピーして、テキストと項目数だけ差し替える
4. 座標・フォントサイズ・配色は原則そのまま。要素が増減するときだけ、同じリズム（行間・余白の刻み）で調整する

5. **実案件のワイヤーとして納品するときは、部品を並べただけにしない。** `_page/starter.html` をコピーし、「型を組み合わせて1ページのワイヤーを作るとき」に従って1枚のHTMLに組み直す。見出しはヒーローで決めたスケールに全セクションを合わせ、余白も共通の刻みにそろえる

新しいスクリーンショットやURLからトレースする場合は、末尾の「新規セクションの作り方」に従う。

## 共通仕様

- セクションは **1440×800 固定**。`.stage` の中の `.wf` に絶対配置し、JSでビューポートに合わせて等倍スケールする
- **CSSは共通ファイルにせず、各HTMLの `<style>` に直書きする**（1ファイル単体で完結させる）。外部読み込みは Google Fonts と Font Awesome の CDN のみ
- **コンテンツ幅は基本 90%**＝ `1440 × 0.9 = 1296px`、左右余白はそれぞれ `72px`。CSS変数 `--wf-content` / `--wf-pad` を使い、数値を直書きしない
- **背景は基本 白**。グレーや黒に変えるのは、そのままだと視認性に問題が出る場合だけ（例：全面画像の上に白抜き文字が乗るMV）
- 色は **白・黒・グレーのみ**
- 和文 **Noto Sans JP** ／ 欧文・数字 **Poppins**（`class="en"`）／ アイコン **Font Awesome 6 無料版**
- 写真・図版はグレーの矩形（`.img`）に置き換える。役割がある場合は `data-l="THUMB"` のようにラベルを付ける（右下に小さく出る）
- ロゴは `LOGO` テキスト（Poppins）。シンボルもグレーボックスも置かない
- 本文はダミーテキスト。**横並びで位置が連動する要素（ヘッダーナビ等）は元の文字数に合わせる**
- コピーライトは `© 20XX Company.` で統一
- **行頭に全角スペース（U+3000）を残さない。左端は必ず上下の要素と揃える。**
  全角スペースは CSS の空白圧縮の対象外なので、区切りに使うと折り返したときに行頭へ残り、1文字分だけ右にずれる。
  - 区切りが要るときは要素を分けて `.sep`（`margin-right: 1em`）で空ける。全角スペースは書かない
  - 段落の字下げなどでどうしても行頭に置く場合だけ `.flush`（`text-indent: -1em`）で打ち消す
- 装飾（写真・グラデーション・影・ホバー・アニメーション）は再現しない。角丸と罫線だけ実測で合わせる
- **スライドショー・カルーセル・ティッカー・マーキーは動かさない。中央に静止させた状態でレイアウトだけを見せる**（見切れさせない）
- **サイト固有の変則レイアウトは、そのまま型にしない。** 型はあくまで「他の案件でも使い回せるオーソドックスな骨格」であること。
  次のようなものは**汎用形に正規化してから**収録する（正規化できないものは収録しない）。
  - 装飾目的のパララックス帯やタイルの敷き詰め → 素直な画像グリッドにする
  - 本文に画像を重ねる、円形に切り抜くなどの造形 → 矩形の2カラム or カード並びにする
  - 数値やカードが不揃いな位置に散る配置 → 等分グリッド＋罫線で揃える
  - そのサイトのコピー画像ありきで組まれたブロック → 見出し＋本文＋署名など、テキストで成立する構成に置き換える

### 縦位置のルール

- **MV・下層ページタイトル・フッター以外のセクションは、アートボードの上下中央に配置する**
  （コンテンツの高さを出し、`top = (800 − コンテンツ高さ) ÷ 2`）
- **MV** は下端揃え（見出し・リード文はアートボード下辺基準）
- **フッター** は実測どおりの縦位置

### 実測から確定している基準サイズ

| 用途 | サイズ | 行間 |
|---|---|---|
| ヘッダーナビ | 12px / 500 | — |
| MV 大見出し | 56px / 700・letter-spacing 0.14em | 70px |
| MV リード文 | 12px | 24px |
| セクション見出し（和文） | 40px / 700 | 1 |
| セクション見出し（欧文 `/ Xxx`） | 24px / 600 | 1 |
| 一覧の日付・カテゴリ・タイトル | 14px | — |
| ボタン内テキスト（小 / 大） | 12px / 14px | — |
| 住所・SNS・コピーライト | 13px | 22.5px |
| 電話番号 | 36px / 500 | 1 |

### 実測済みの共通レイアウト値

| 項目 | 値 |
|---|---|
| コンテンツ枠 | left 72 / 幅 1296（右端 1368） |
| ヘッダー | top 14 / 高さ 60。ロゴ左寄せ、ナビ＋ハンバーガーを右寄せ（gap 30） |
| ヘッダー：白ピルナビ | 794×60 / radius 30、内側 padding-left 25、項目間 gap 32 |
| ヘッダー：CTA（黒） | ピル内 left 660 / top 12 / 117×36 |
| ヘッダー：ハンバーガー | 60×60 円（右端 1368 揃え） |
| 一覧1行（news） | 高さ 91 / 下罫線 1px。行内オフセット：日付 0・カテゴリ 106・タイトル 300 |
| バナー3カラム（footer） | 412×205 / gap 30 / radius 24 |

## 型を組み合わせて1ページのワイヤーを作るとき

このライブラリの各HTMLは**部品**。実案件のワイヤーとして納品するときは、部品をそのまま並べるのではなく、次の3点を必ず守って**1枚に組み直す**。

雛形と見本を用意してある。まず `_page/starter.html` をコピーして始める。
- [`_page/starter.html`](_page/starter.html) … 変数・セクション枠・見出しコンポーネントだけを持つ空の雛形
- [`_page/sample.html`](_page/sample.html) … 6型（col3-001 / col2-021 / col4-004 / col5-004 / news-001 / footer-007）を実際に組んだ見本

### 1. 1ページ＝1つのHTMLファイル

- セクションごとにファイルを分けない。ヒーローからフッターまでを**縦につながった1枚のHTML**にする
- アートボード用の `1440×800` 固定と `.stage` / `.wf` のスケール処理（`--s` の JS）は**使わない**。ページは幅1440pxの縦長1カラムとして素直に流し、各セクションの高さは中身なりにする
- CSSは同じHTMLに直書き。変数とユーティリティは先頭に**1回だけ**置き、その下にセクション固有のCSSを型IDのプレフィックス（`.col3_001__…`）で並べる。どの型から持ってきたかが後から辿れるようにする
- カタログの各型が持つ `top: ○○px`（アートボード上下中央のための値）と `.wf__inner` の絶対配置は**すべて捨てる**

### 2. 見出しはヒーローで決めた1つのスケールに全セクションを合わせる

**型ごとの見出しはバラバラ**（167型で主見出しのサイズが35種類ある）。そのままつなぐとページがちぐはぐになる。**位置は型のまま、スケール（サイズ・行間・ラベル）だけをページ共通にそろえる**のが原則。位置を動かさないので、レイアウトは崩れない。

#### 寄せるのは見出しと、その直下のリードまで

**これだけを統一する**

| 変数 | 値 | 用途 |
|---|---|---|
| `--fs-hero` | 48px / lh70 | ヒーローと、ページに1〜2箇所の見せ場だけ |
| `--fs-sec` | 32px / lh46 | セクション見出し（標準） |
| `--fs-label` | 14px / lh20 / .12em / グレー | 見出しの上の英字ラベル |
| `--fs-lead` | 16px / lh28 | 見出しの直下のリード1本 |

**これは統一しない。型の実測のまま置く**

カード内の小見出し・本文・キャプション・日付・タグ・注記・数字。型によって 13 / 14 / 15 / 16 / 18 / 19 / 22px などバラバラだが、**そのままにする**。ここを寄せると折り返し行数とカード高さが変わり、型の実測値（カード幅・gap・画像寸法とのバランス）が崩れる。

> 例：`col3-001` のカード本文は 15px/lh30。これを `16px/lh28` に寄せてはいけない。`col5-004` の左リード 15px/lh24 も同じ。

#### 見出しコンポーネントは3つだけ

```html
<div class="sh">…</div>            <!-- 上・左揃え（標準） -->
<div class="sh sh--center">…</div> <!-- 上・中央揃え -->
<div class="sh sh--hero">…</div>   <!-- 特大。ページに1〜2箇所まで -->
```

- 「英字ラベル → 和文見出し」か「和文見出し → 英字サブ」か、順序をページ内で1つに決める
- 中央揃え・左揃えもページ内で1つに決める。型によって揃え方が違っても、採用した側に寄せる
- 英字ラベルの表記（全大文字か Capitalize か）もそろえる

#### 型が持つ見出しの扱い

| 型のつくり | どうするか |
|---|---|
| 見出しを持たない | 上に `.sh` を載せる |
| 上に見出しを持つ | その見出しを削って `.sh` に差し替える |
| **見出しが本体と横並び**（左右2カラムの片側にある） | **`.sh` は使わない。**位置はそのままで、見出しに `.sh-inline__label` / `.sh-inline__title` を当ててスケールだけ合わせる |
| 巨大英字が意匠として効いている（`col2-006` の背面250px など） | その型の見出しを残す。ページに1〜2箇所まで |

#### 見出しに使える幅（実測）

スケールを落としたときに入るかどうかは、**机上で判定できる**。和文は「幅 ÷ フォントサイズ」がだいたいの1行あたり文字数（`--fs-sec` 32px なら 336px で10文字）。

**MV** … 各型の実測値はカタログの行末に「**見出し枠 幅◯◯**」として入れてある。狭いものだけ再掲。

| 型 | 見出しに使える幅 | 32pxなら | 48pxなら |
|---|---|---|---|
| `mv-010` | 397 | 12文字 | 8文字 |
| `mv-008` | 448 | 14文字 | **9文字** |
| `mv-003` | 453 | 14文字 | **9文字** |
| `mv-001` | 511 | 15文字 | 10文字 |
| `mv-014` | 558 | 17文字 | 11文字 |
| `mv-011` | 582 | 18文字 | 12文字 |

**見出しが本体と横並びの型**（`.sh` に差し替えると2カラム構造が壊れる。位置は動かさない）

| 型 | 見出しに使える幅 |
|---|---|
| `col1-009` | 1296 |
| `col2-002` | 560 |
| `col2-003` | 464 |
| `col2-005` | 560 |
| `col2-006` | 586 |
| `col2-007` | 704 |
| `col2-010` | 480 |
| `col2-011` | 412 |
| `col2-012` | 783 |
| `col2-013` | 555 |
| `col2-014` | 160 |
| `col2-015` | 636 |
| `col2-016` | 548 |
| `col2-019` | 394 |
| `col2-020` | 293 |
| `col2-026` | 579 |
| `col2-027` | 335 |
| `col2-030` | 424 |
| `col2-031` | 644 |
| `col2-036` | 500 |
| `col2-039` | 385 |
| `col2-040` | 470 |
| `col2-041` | 336 |
| `col2-045` | 336 |
| `col2-047` | 580 |
| `col2-048` | 400 |
| `col3-012` | 366 |
| `col3-013` | 264 |
| `col3-022` | 336 |
| `col4-002` | 310 |
| `col4-007` | 226 |
| `col4-015` | 336 |
| `col5-004` | 336 |
| `news-003` | 300 |
| `news-004` | 380 |
| `news-005` | 300 |
| `news-006` | 293 |
| `news-008` | 336 |
| `news-010` | 402 |

#### 中央揃えの見出しを持つ型

ページを左揃えでそろえるなら、これらは左揃えに直す（逆も同じ）。

```
col1-011  col1-013  col1-016  col1-019  col2-004  col2-025  col2-037  col2-038
col3-009  col3-017  col3-018  col3-021  col3-024  col3-025  col4-001  col4-008
col4-009  col4-010  col5-002  col5-003  col5-005  col5-007  col5-008  col5-010
```

#### 遷移サインの扱い

リンク行・カードの**矢印や丸ボタンは「押すと遷移する」というサイン**。リンクでない用途（対比リスト・仕様の列挙・実績の羅列など）に型を流用するときは**外してよい**。残すと全項目がリンクに見える。`col2-046` `col4-011` `col4-014` `col2-016` `col2-017` `col3-021` などが該当する。

### 3. 余白を統一する

型ごとの余白は実測値なのでバラバラ。ページに組むときは**共通の刻みを1つ決めて全セクションに適用する**。

| 変数 | 値 | 箇所 |
|---|---|---|
| `--sp-sec` | 120px | セクションの上下パディング |
| `--sp-sec-lg` | 160px | 濃色・全面画像のセクションの上下 |
| `--sp-head` | 48px | 見出しブロック → 本体 |
| `--sp-card` | 48px | カード間（4カラム以上は 32px） |
| `--sp-lead` | 16px | 見出し → リード |

- セクションの区切りは**背景色の切り替え**（白 → 明グレー → 白）でつくる。区切り線は足さない
- 左右の余白は全セクションで `left 72 / 幅 1296` を守る。型によって幅が違う場合は1296側に合わせる
- **全幅の帯**（`col2-035` `col1-014` `col2-033` など幅1440のもの）は `.sec--bleed`（`padding:0`）を使う。`.sec` の120pxを付けない
- **本体の寸法（カード幅・gap・画像サイズ・行高・罫線）は実測値のまま触らない。** ここが型の価値なので、統一の対象は見出しと外側の余白だけ

### 4. 組み終わったら3つ検算する

DevTools のコンソールに貼って確認する。1つでも引っかかったら直す。

```js
// ① 横スクロールが出ていないか
console.log('width', document.documentElement.scrollWidth, '← 1440 でなければはみ出している');

// ② .sec__inner の左端が全部 72 で揃っているか
console.log('left', [...new Set([...document.querySelectorAll('.sec__inner')]
  .map(e => Math.round(e.getBoundingClientRect().left)))], '← [72] だけになるはず');

// ③ 箱から中身が溢れていないか（縦・横とも）
console.log('overflow', [...document.querySelectorAll('body *')].filter(e => {
  const cs = getComputedStyle(e);
  if (cs.overflow !== 'visible' || cs.display === 'inline') return false;
  const r = e.getBoundingClientRect();
  if (r.width < 2 || r.height < 2) return false;
  return [...e.children].some(c => {
    if (getComputedStyle(c).position === 'absolute') return false;
    const cr = c.getBoundingClientRect();
    return cr.bottom > r.bottom + 2 || cr.right > r.right + 2;
  });
}).map(e => e.className || e.tagName), '← [] になるはず');
```

```js
// ④ 型より要素が減った枠が、空のまま残っていないか
//    アイコン（<i>）・画像・グレーボックスだけの枠は中身ありとして数える
console.log('empty', [...document.querySelectorAll(
  '[class*="__c"],[class*="__b"],[class*="__cell"],[class*="__col"],[class*="__card"],li')]
  .filter(e => !e.textContent.trim() && !e.matches('.img') && !e.querySelector('img,svg,i,.img,[data-l]'))
  .map(e => e.className || e.tagName), '← [] になるはず');
```

**⑤ 原稿の文が落ちていないか。**これはブラウザではなく手元で突き合わせる。`tools/check-copy.mjs` に
「元原稿のHTML」と「組んだワイヤー」を渡すと、落ちた文を一覧で出す。

```
node tools/check-copy.mjs 元原稿.html 組んだワイヤー.html
```

型が受けられる件数を超えた情報を入れようとすると、たいてい**原稿のほうを削って収めてしまう**。
それが起きていないかを最後に必ず見る。落ちていたら文を戻し、型を選び直す。

③は「子が親の箱からはみ出している」だけを見るので、余計な検出は出ない（`scrollHeight > clientHeight` だと `line-height:1` の文字が全部引っかかる）。

ただし**絶対配置どうしの「重なり」は③では見つからない**。MVのように見出しとビジュアルが兄弟で重なっている型がこれにあたるので、そこは上の「見出しに使える幅」の表で**組む前に**潰しておく。

## カタログ

セクションは**カラム数**で分類している（`pagetitle` `mv` `news` `footer` は役割が固定されるため別立て）。
「用途」列は元にした実サイトでの役割。テキストはすべてダミーなので、用途は目安として使い、まずカラム数で選ぶ。

### 下層ページタイトル `pagetitle/`

| id | 背景 | 構造 |
|---|---|---|
| pagetitle-001 | 全面画像 | 下層ページのタイトル部。左上にパンくず12px、中段に英字96px/500＋和文28px＋リード16px |
| pagetitle-002 | 明グレー | 画像を使わないページタイトル。パンくず12px＋英字ラベル＋和文44px＋区切り罫線＋リード |
| pagetitle-003 | 白 | 見出しが上・帯画像1296×300が下。パンくず＋和文48pxの見出し、右にリード |

### MV / メインビジュアル `mv/`

| id | 背景 | 構造 |
|---|---|---|
| mv-001 | 全面画像（グレー） | 左上ロゴ、右上に白ピルナビ＋黒CTA＋ハンバーガー。左下に大見出し2行、その下に縦罫線つきのリード文3行＋スライドカウンター。**見出し枠 幅511** |
| mv-002 | 白 | 高さ100のヘッダー（ロゴ左／ナビ＋黒ピルCTA右）。中央に64pxのキャッチコピー1行＋サブ、下端にアイコン付きティッカー（中央静止）。**見出し枠 幅1213** |
| mv-003 | 明グレー | 高さ101・下罫線のヘッダー（ロゴ／ナビ／言語切替／角型CTA／ハンバーガー）。左に80pxの見出し2行＋サブ、右に大きな画像枠、下端に白のお知らせバー。**見出し枠 幅453** |
| mv-004 | 全面画像 | 透過ヘッダー（ロゴ／ナビ／言語／CTA）。右寄せの50pxコピー2行（行間81.5）のみのシンプルなMV。**見出し枠 幅727** |
| mv-005 | 全面画像 | ロゴ＋白ピルナビ（高さ58 r29）＋言語切替。中央に斜体76pxの英字コピー2行＋和文サブ、右端に縦のドットページャー。**見出し枠 幅1368** |
| mv-006 | 濃色 | 左上に白いロゴブロック（260×122）、半透明のピルナビ＋白い言語ボタン。中央に和文28pxコピー＋72pxのロゴテキスト。**見出し枠 幅1368** |
| mv-007 | 全面動画 | 固定ヘッダー（ロゴ＋英字サブ／ナビ7項目14px／右に採用リンク）。左下に和文48pxのコピー＋英字1行のミニマルなMV。**見出し枠 幅782** |
| mv-008 | 白 | 四隅を角丸にしたビジュアル（1296×660）の上に、左下寄せで英字72px/200のラベル＋和文ラベル＋42pxコピー＋本文＋テキストリンク。**見出し枠 幅448** |
| mv-009 | 全面画像 | 高さ80のヘッダー（ロゴ＋サイト名／ナビ6項目／右にエントリー）。ビジュアル下端に和文80pxのコピー。**見出し枠 幅1332** |
| mv-010 | 全面画像 | ナビ左寄せ＋右にMENUのヘッダー。右下に和文48px/700 lh55.5 の大コピー2行だけを置く。**見出し枠 幅397** |
| mv-011 | 全面画像 | 高さ80のヘッダー（ナビ8項目13px/600）。左下に和文56px/600のコピー1行、右下に英字ラベル＋小テキスト。**見出し枠 幅582** |
| mv-012 | 全面画像 | 上下中央に和文32px/700のコピー、下部にNEWSバー（ラベル＋日付＋見出し）。**見出し枠 幅1368** |
| mv-013 | 白 | 左右に余白を残した角丸の大ビジュアル1396×700。左下に枠線の丸ボタンのみを置く最小構成。**見出しを持たない型**（コピーを足すなら枠は幅1296） |
| mv-014 | 白 | 高さ128のヘッダー（ロゴ／英字ナビ5項目／Contactボタン）。左に英字88px/200の3行コピー、右に和文リード。下に幅いっぱいのビジュアル＋丸ボタンφ150。**見出し枠 幅558** |

### 1カラム `col1/`

全幅1本。横に並ぶ要素がない構成。コピーや図版1点を主役にするとき

| id | 用途 | 背景 | 構造 |
|---|---|---|---|
| col1-001 | コンセプト | 白 | 英字ラベル＋36pxのリード3行（強調部分だけグレー）。下段は左に小見出し、右に幅403の補足テキスト |
| col1-002 | コンセプト | 全面画像 | 左下寄せに英字58px/900の見出し＋リード2行だけを置く、写真主役のステートメント |
| col1-003 | コンセプト | 明グレー | 英字24pxラベル＋32px/500 lh56 のスローガン2行だけの1カラム。企業スローガンの見せ場に |
| col1-004 | 会社紹介 | 明グレー | 全幅1カラム。英字24pxラベル＋32px/500 lh56の見出し2行＋16pxリード＋中央のテキストボタン |
| col1-005 | メッセージ | 全面画像 | 英字64pxの見出し＋役職18px・氏名24px＋本文＋テキストボタン。代表メッセージの導入に |
| col1-006 | 特長 | 全面画像 | 見出しブロック（英字14→和文28→本文14×3行）だけを左に置き、右下にテキストリンク |
| col1-007 | レポート | 白 | ラベル＋英字70pxの見出し＋18pxのリード2行。下に幅いっぱい・角丸24の大きなビジュアルカード |
| col1-008 | ビジュアル1点 | 白 | 上に英字90pxの見出し、下に右寄せの大きな画像枠1点。**ロゴを並べる骨格ではない**（ロゴ一覧は `col5-008`） |
| col1-009 | サービス | 白 | 左上に英字90pxの見出し。右73%のカラムに「英字52px＋和文ラベル」の小見出しと、番号8%／タイトル41%／本文44%の行リスト3件（下罫線）。**3件**（行を増減できる） |
| col1-010 | インタビュー | 白 | 上にアイキャッチ文＋英字90pxの大見出し＋リード。下に「写真477×330＋テキスト358」のカルーセル1件を中央に静止表示 |
| col1-011 | 採用 | 全面画像 | 背景に1枚の大きな画像を敷き、中央にラベル＋英字80px＋本文＋丸ボタン |
| col1-012 | お問い合わせ | 全面画像 | 英字74pxの見出し＋リード3行＋半透明の箇条書きパネル（580幅） |
| col1-013 | お問い合わせ | 全面画像 | 中央揃えで英字88px/500の特大コピー＋24pxリード＋テキストボタン。採用エントリー導線の締めに使う |
| col1-014 | 誘導バナー | 全面画像 | 全幅の大バナー（高さ262）を縦に2本。左に英字56px＋和文、右端に丸アイコン。**2件**（行を増減できる） |
| col1-015 | 絞り込み | 明グレー | 見出し＋「◯◯から探す」のタグ群を2グループ。高さ30・角丸のタグを折り返しで敷き詰める絞り込みブロック。**8件**（折り返しで増減できる） |
| col1-016 | 言い切り | 白 | 中央に48px/400の見出し2行＋リード20px（幅640）＋枠線ボタン188×40。前後を選ばない「言い切り＋導線」 |
| col1-017 | 会社概要 | 白 | 全幅の定義リスト8行（項目名240px＋内容・行高64・上下罫線）。和文コーポレートの必須セクション。**8件**（行を増減できる） |
| col1-018 | 募集要項 | 白 | タグ＋職種名24px＋定義リスト6行（項目名200px）＋中央に応募ボタン300×56。**6件**（行を増減できる） |
| col1-019 | お問い合わせ | 白 | 幅800中央の入力フォーム（ラベル＋必須バッジ＋枠線の面52／テキストエリア120）＋同意チェック＋送信ボタン。**4件**（行を増減できる） |
### 2カラム `col2/`

左右2分割。テキスト｜画像、見出し｜本文など。もっとも汎用的

| id | 用途 | 背景 | 構造 |
|---|---|---|---|
| col2-001 | 会社紹介 | 白 | ドット付きラベル＋英字70pxの見出し。下を2カラムに分け、左に36pxの和文リード2行、右に19px lh37.5 の長文3ブロック。**3件**（行を増減できる） |
| col2-002 | 会社紹介 | 明グレー | 左に32pxの見出し2行＋本文3行＋ピルボタン、右に3枚の画像を重ねたコラージュ |
| col2-003 | 会社紹介 | 明グレー | 左に英字72px/200の大ラベル＋和文、右に28px/500 lh50.4 の大きめリード |
| col2-004 | 会社紹介 | 明グレー | 中央にラベル＋48px/700の見出し。下は左に大きな画像、右に35px/700の見出し＋本文＋リンク2本。**2件**（行を増減できる） |
| col2-005 | 会社紹介 | 白 | 左に小ラベル＋72px/600の大見出し＋本文＋丸ボタン、右に画像663×657 |
| col2-006 | 会社紹介 | 白 | 背面に250pxの巨大英字（静止）、左に58px/200の見出し、右に20px/300 lh48 の本文2ブロック。**2件**（行を増減できる） |
| col2-007 | メッセージ | 白 | 社長挨拶・理念メッセージの定番型。左に人物画像520×600、右に英字ラベル＋32pxの見出し＋本文＋署名（役職14px／氏名22px） |
| col2-008 | メッセージ | 白（帯画像） | 高さ382の帯状ビジュアルの上下中央に、左に和文40px/700の見出し、右に本文20px/700＋丸ボタンφ64 |
| col2-009 | 特長 | 白 | 画像630×300＋「見出し20px＋丸ボタン」＋本文の2カラム。**2件** |
| col2-010 | 特長 | 明グレー | 左に英字64pxの見出し＋下罫線のリンク行2本＋テキストボタン、右に画像480×452。**2件**（行を増減できる） |
| col2-011 | 特長 | 白 | 左に英字64pxの見出し・右にリード、下に幅いっぱいの図版（1296×233）＋中央のテキストボタン |
| col2-012 | 特長 | 白 | 左に小画像300×187、右に40px/700の見出し＋リード。下に2カラムのカード（画像421×249＋見出し24px＋本文）。**2件** |
| col2-013 | 特長 | 明グレー | 左に画像663×609、右に小ラベル＋40px/600の見出し＋本文＋丸ボタン |
| col2-014 | 沿革 | 明グレー | 左に英字20pxラベル＋和文32pxの見出し、右にリード。下に幅いっぱい（1296×474）の年表図版 |
| col2-015 | 会社情報 | 白 | 左に英字70pxの見出し＋30pxの和文2行＋リード、その下に「サムネ70角＋和文＋英字＋矢印」の103pxリンク行3件。右に大きな画像枠。**3件**（行を増減できる） |
| col2-016 | 会社情報 | 白 | 左に英字64pxの見出し＋下罫線のリンク行4本（ピッチ88）、右に画像508×478。**4件**（行を増減できる） |
| col2-017 | 会社情報 | 明グレー | 左に画像2枚をずらして重ね、右に下罫線のリンク行6本（20px/700・ピッチ70）＋右下にリンク＋丸φ64。**5件**（行を増減できる） |
| col2-018 | サービス | 白 | ラベル＋英字80pxの見出し。下に「番号／画像574×330／右にテキスト＋ピルボタン」の横並び1件。**2件**（行を増減できる） |
| col2-019 | サービス | 白 | 左に全面画像608×800＋白抜きの見出し、右に「番号（01/06）＋画像493×260＋見出し24px」のカードを縦積み。**2件**（行を増減できる） |
| col2-020 | サービス | 白 | 左に英字72px/200の見出し＋リンク、右に「画像280×210＋事業名36px＋サービス名＋本文」の行を3つ（ピッチ235）。**3件**（行を増減できる） |
| col2-021 | サービス | 白 | 左端に接地する縦長画像448×640、右に英字64px＋和文20px＋本文16px lh28＋テキストボタン |
| col2-022 | サービス | 白 | 見出しの下に「番号＋見出し24px＋本文（幅788）＋右に画像359×202」の行を並べる。**2件**（行を増減できる） |
| col2-023 | サービス | 白 | 英字76px/700 lh68 の特大見出し2行＋2カラムのカード（画像620×280＋見出し26px＋丸アイコン＋本文）、右下にリンク。**2件** |
| col2-024 | サービス | 明グレー | 英字130px/100の見出し。左にテキスト（30px/300の見出し＋英字＋本文＋View all）、右に画像735×420。**2件**（行を増減できる） |
| col2-025 | インタビュー | 白 | 中央見出し＋大小カードの組み合わせ。左に大カード（画像541×300）、右に小カード（画像218×156）を2枚。**2件**（行を増減できる） |
| col2-026 | インタビュー | 明グレー | 対談（クロストーク）型。左に集合写真641×401、右に見出し20px＋出演者3名のリスト。**3件**（折り返しで増減できる） |
| col2-027 | お客様の声 | 白 | 左に英字56pxの見出し＋本文＋丸矢印つきMoreリンク、右に大きな画像730×486 |
| col2-028 | 導入事例 | 白 | 英字130pxの見出し＋正方形画像392角＋右に日付・タグ・見出し。左下にPrev/Next、右下にView all |
| col2-029 | 採用 | 白（濃色パネル） | 全幅のカードを左右分割。左の濃色パネルにラベル＋見出し2行＋本文＋白ボタン、右に画像 |
| col2-030 | 採用 | 明グレー | 左に英字72px/200＋42pxコピー＋本文＋リンク、右に255×307のタイルを2列×2行のグリッドで（元は流れるタイル→静止グリッドに正規化）。**4件**（折り返しで増減できる） |
| col2-031 | お問い合わせ | 白（濃色パネル） | 幅いっぱい・角丸16の濃色パネル。左にラベル＋英字70pxの見出し＋リード2行、右に白い角型ボタン |
| col2-032 | お問い合わせ | 全面画像 | 英字56pxの見出し＋本文16px/600（幅667）、右に枠線のボタン276×76 |
| col2-033 | 誘導バナー | 全面画像 | 画面を左右2分割した誘導バナー。各パネル左下にラベル＋英字46pxの見出し、右下に丸ボタンφ62。**2件** |
| col2-034 | 誘導バナー | 全面画像 | 左右2分割し、各パネル中央に英字64px/200＋和文14px/600だけを置く最小構成のバナー。**2件** |
| col2-035 | 誘導バナー | 全面画像 | 2分割の誘導ブロック。英字36px/400＋和文16px/600 だけの最小構成。**2件** |
| col2-036 | 下層導線 | 明グレー | 左に42pxの見出し＋リード＋リンクボタン2列×4行、右に「日付（日.月／年）＋タイトル」の一覧3件。**8件**（折り返しで増減できる） |
| col2-037 | 拠点一覧 | 白 | 中央に和文＋英字70pxの見出し。下に「区分見出し（幅180）＋項目を横並び」の定義リストを罫線で4行。**7件**（折り返しで増減できる） |
| col2-038 | 読みもの | 白 | 画像の横に縦書きの見出し（32px・幅45）を添えた記事カードを2列。特集を2本並べるときに。**2件** |
| col2-039 | 会社紹介 | 明グレー | 左に画像2枚をずらして重ね、右に英字ラベル＋32px/700の見出し＋本文2ブロック＋リンク。**2件**（行を増減できる） |
| col2-040 | データ | 白 | 左に48px/400の見出し3行＋リード＋注釈、右に画像700×644 |
| col2-041 | イベント | 白 | 左に固定336pxの見出しブロック（32px＋リード＋テキストリンク）、右に横型カード2列×2行（サムネ220角＋タイトル＋日時・会場）。**4件**（折り返しで増減できる） |
| col2-042 | 特長 | 白 | 見出し上＋2列×2行のテキストブロック4件（上罫線＋見出し22px＋本文16px）。写真がまだ無い段階の設計に。**4件**（折り返しで増減できる） |
| col2-043 | 一覧 | 白 | 見出し上＋2列×3行の横型カード6件（サムネ140角＋タイトル17px＋日付・タグ）。**6件**（折り返しで増減できる） |
| col2-044 | FAQ | 白 | 見出しを置かない2列×3行のテキストカード6件（番号＋見出し20px＋本文）。用語・手順の一覧に。**6件**（行を増減できる） |
| col2-045 | 特集 | 白 | 左に固定336pxの見出しブロック＋右に大きめカード2枚（画像440×280＋見出し22px＋本文）。**2件** |
| col2-046 | 下層導線 | 白 | 2列×4行のリンク行8項目（和文18px＋英字ラベル＋右端に矢印・上下罫線）。**8件**（行を増減できる） |
| col2-047 | アクセス | 白 | 左に地図660×420、右に住所・TEL＋交通手段リスト3件（アイコン＋見出し15px＋本文）。**3件**（行を増減できる） |
| col2-048 | お問い合わせ | 白 | 左に見出し・リード・電話パネル（幅400）、右にフォーム。上段は氏名／会社名を横2列に。**3件**（行を増減できる） |
| col2-049 | 一覧 | 白 | 見出し上＋2列×4行の横型カード8件（サムネ100角＋タイトル16px＋補足・罫線区切り）。**8件**（折り返しで増減できる） |
### 3カラム `col3/`

3列の繰り返し。カード・記事・グリッドなど並列の項目を見せるとき

| id | 用途 | 背景 | 構造 |
|---|---|---|---|
| col3-001 | 特長 | 白 | 特長・強みの定番型。英字ラベル＋和文40pxの見出し＋リード、下に3カラムのカード（画像400×260＋番号＋見出し20px＋本文）。**3件** |
| col3-002 | サービス | 白 | 英字56pxの見出し＋右にリード。下に大カード638×480＋小カード317×238を4枚組み合わせたグリッド。**5件**（行を増減できる） |
| col3-003 | インタビュー | 白 | 見出し＋3カラムのカード（画像383×383の左上にタグ・左下に部署、下に見出し18px＋氏名）。**3件** |
| col3-004 | インタビュー | 白 | 英字90px/500の見出し＋右上にView All、下に縦長カード3枚（画像360×330＋見出し＋部署＋氏名）。**3件** |
| col3-005 | 導入事例 | 白 | コンパクトな見出し＋3カラムのカード（サムネ／タグ／タイトル／社名・日付）。3枚とも開始位置を揃える。**3件**（折り返しで増減できる） |
| col3-006 | 導入事例 | 白 | 縦長カード3枚（画像290×435）。画像上に番号・地域・日付を重ね、下部にタイトル18px＋英字。**3件** |
| col3-007 | メディア | 白（グレー面） | グレーのパネルに英字40px/900の見出し＋リンク、下を1px罫線で区切った3カラムの記事グリッド（画像＋タイトル＋右下に日付）。**3件**（折り返しで増減できる） |
| col3-008 | メディア | 白 | 見出し＋記事カード3枚（画像383×215＋角丸タグ＋日付＋見出し18px）。スライダーは中央静止。**3件** |
| col3-009 | メディア | 白 | 中央にラベル＋48pxの見出し＋リード。下に3カラムの記事カード（画像380×244＋日付＋見出し＋本文）。**3件** |
| col3-010 | メディア | 白 | 英字130pxの見出し＋左にカテゴリ、中央に画像434×289、右に日付＋見出し20px。**3件**（行を増減できる） |
| col3-011 | ギャラリー | 白 | 画像グリッドの定番型。英字ラベル＋和文40pxの見出しの下に、3列×2行のセル（画像416×230＋キャプション15px）。**6件**（折り返しで増減できる） |
| col3-012 | ギャラリー | 白 | 左に英字90pxの見出し＋View All、右に正方形246角を3列×2行（元は縦に流れるスライダー→静止グリッドに正規化）。**6件**（折り返しで増減できる） |
| col3-013 | ムービー | 白 | 左に見出しブロック（幅264）＋テキストリンク2本、右に縦長サムネ270×478を gap18 で3枚。**3件** |
| col3-014 | 誘導バナー | 白 | 縦長カード3枚（427×420）を上下中央揃えで並べる。画像下に英字50px＋和文14px。**3件**（行を増減できる） |
| col3-015 | 学びの流れ | 白 | 大きな番号80px/800を画像の右上に重ねた3カラム（画像310角＋見出し26px＋本文）。**3件** |
| col3-016 | イベント | 明グレー | 英字50pxの見出し＋イベントカード3枚（年＋枠線タグ＋タイトル15px＋「開催日」の定義リスト）。**3件** |
| col3-017 | 導入事例 | 白 | 中央見出し＋事例カード3枚（画像392×220＋業種13px＋見出し16px lh1.7＋2行のクレジット＋右下に丸矢印）。**3件** |
| col3-018 | コラム | 白 | 中央にラベル＋見出し26px。画像を持たない記事カード3枚（日付14px＋タイトル20px＋抜粋16px lh1.75）＋一覧ボタン。**3件** |
| col3-019 | お知らせ | 白 | 見出し上＋3列×2行の記事カード6件（画像400×180＋日付＋タグ＋タイトル18px）。一覧ページ本体にも。**6件**（折り返しで増減できる） |
| col3-020 | 事業一覧 | 白 | 見出し上＋3列×3行のテキスト項目9件（番号＋見出し20px＋本文）。画像を使わない列挙に。**9件**（折り返しで増減できる） |
| col3-021 | メニュー | 白 | 見出し上＋3列×2行の正方形カード6件（画像＋見出し19px＋丸矢印）。カテゴリ選択に。**6件**（折り返しで増減できる） |
| col3-022 | 実績 | 白 | 左に固定336pxの見出しブロック＋右に3列×2行のカード6件（画像288×192＋タイトル）。**6件**（折り返しで増減できる） |
| col3-023 | サービス | 白 | 見出し24px＋本文が上、画像400×260が下の3カラム。読ませてから見せる並び。**3件** |
| col3-024 | 資料DL | 白 | 表紙画像400×240（枠線）＋タイトル18px＋本文＋DLボタンのカード3枚。**3件** |
| col3-025 | 料金 | 白 | 枠線カード3枚でプラン比較（プラン名18px＋価格40px＋機能リスト5行＋ボタン）。中央だけ黒枠で推奨を示す。**5件**（行を増減できる） |
| col3-026 | 事業一覧 | 白 | 見出し上＋3列×4行のテキスト項目12件（上罫線＋見出し18px＋本文14px）。項目数が多い列挙に。**12件**（折り返しで増減できる） |
### 4カラム `col4/`

4列の繰り返し。項目数が多く、1項目あたりの情報が少ないとき

| id | 用途 | 背景 | 構造 |
|---|---|---|---|
| col4-001 | 特長 | 明グレー | 中央にラベル＋48pxの見出し＋リード。下に画像280×175＋見出し18pxのカードを横並び（中央静止）。**4件** |
| col4-002 | 会社情報 | 白 | 左に英字44px/900の見出し2行、右に4カラムのカード（画像＋見出し＋右下に丸ボタン）。**4件** |
| col4-003 | 会社情報 | 明グレー | 英字130pxの見出し＋幅1210のリード。下にカード4枚を上揃えで（画像288×250＋番号＋英字＋和文）。**4件**（行を増減できる） |
| col4-004 | 数字で見る | 明グレー | 数字で見るの定番型。見出し＋リードの下に、縦罫線で等分した4カラム（ラベル16px＋数字60px＋単位20px＋注記13px）。**4件** |
| col4-005 | 商品 | 白 | ラベル＋英字62pxの見出し、右にPrev/Next＋ピルボタン。下に商品カード（303幅・画像206）の横並び（中央静止）。**4件** |
| col4-006 | 採用 | 白 | ラベル＋英字70pxの見出し＋36pxの和文2行、右上に角型ボタン。下に写真4枚のスライダー（中央静止）。**4件** |
| col4-007 | 採用 | 白 | 左に見出しブロック（幅226）、右に4カード（H3 18px→英字12px→画像186×135）を gap62.5 で横並び。**4件** |
| col4-008 | トピックス | 白 | 中央見出し＋正方形画像240角のカード4枚（タイトル16px＋グレー面の所属ラベル）。**4件** |
| col4-009 | 取扱一覧 | 白 | 中央に英字＋和文32pxの見出し。下にアイコン＋テキストのリンクを4列で敷き詰める（行高56）。**12件**（折り返しで増減できる） |
| col4-010 | お知らせ | 明グレー | 画像を使わない記事カード4枚（日付14px＋タイトル16px/500＋グレー面のタグ）。サムネがないときに。**4件** |
| col4-011 | カテゴリ案内 | 白 | 縦書きの小見出し24px（幅34）＋罫線区切りのリンク4行（20px＋矢印）を4カラム。和文サイトの索引向け。**4件** |
| col4-012 | 導入事例 | 白 | 円形アイコン126＋グレータグ＋タイトル24px/400（2行）＋2行のクレジット14pxの4カラム。写真ではなくロゴ主体の軽いカード。**4件** |
| col4-013 | ギャラリー | 白 | 見出し上＋4列×2行のカード8件（画像300×200＋キャプション15px＋補足）。**8件**（折り返しで増減できる） |
| col4-014 | カテゴリ案内 | 白 | 見出し上＋4列×3行のリンク索引12項目（和文16px＋矢印・罫線区切り）。**12件**（折り返しで増減できる） |
| col4-015 | ラインナップ | 白 | 左に固定336pxの見出しブロック＋右に4カード（画像210×140＋タイトル＋本文13px）。**4件** |
| col4-016 | 制度・流れ | 白 | 見出し上＋4列×2行のテキストカード8件（番号28px＋見出し18px＋本文14px）。**8件**（折り返しで増減できる） |
| col4-017 | トピックス | 白 | タグ＋見出し20pxが上、画像300×200が下の4カラム。見出しを先に読ませる並び。**4件** |
### 5カラム `col5/`

5列以上の繰り返し。数字や小さなカードを並べるとき

| id | 用途 | 背景 | 構造 |
|---|---|---|---|
| col5-001 | 数字で見る | 白 | 見出し＋リードの下に、実績カード5枚を均等配置（ラベル16px＋数字56px＋単位16px＋注記12px）。**5件** |
| col5-002 | バナー | 明グレー | 中央見出し＋正方形バナー196角を横に6枚（元はスライダー → 静止表示に正規化）。**6件** |
| col5-003 | FAQ | 白 | 中央見出し＋リード。下に質問カード5枚（240×180・18px/700＋開閉アイコン）。**5件** |
| col5-004 | ランキング | 白 | 左に固定336pxの見出しブロック、右に正方形サムネ163のカード5枚（タグ＋タイトル15px＋日付）。**5件** |
| col5-005 | 制度・設備 | 白 | 見出し上＋5列×2行の円アイコンカード10件（円φ120＋見出し15px＋補足12px）。**10件**（折り返しで増減できる） |
| col5-006 | バナー | 白 | 見出しを置かない5列×2行のバナー10枚（画像240×160＋ラベル14px）。**10件**（行を増減できる） |
| col5-007 | フロー | 白 | 見出し上＋縦罫線で等分した5カラム（番号40px＋見出し18px＋本文14px）。5段階の流れに。**5件** |
| col5-008 | ロゴ一覧 | 白 | 見出し上＋6列×3行のロゴ枠18個（196×88）。取引先・導入企業・協賛の並びに。**18件**（折り返しで増減できる） |
| col5-009 | ラインナップ | 白 | 見出しを置かない5列×2行の縦長サムネ10件（画像240×280＋キャプション）。**10件**（行を増減できる） |
| col5-010 | 制度一覧 | 白 | 見出し上＋5列×2行のテキスト項目10件（英字ラベル＋見出し18px＋補足）。アイコンを用意しないときに。**10件**（折り返しで増減できる） |
### お知らせ `news/`

| id | 背景 | 構造 |
|---|---|---|
| news-001 | 白 | 左上に和文大見出し＋英字サブ。下に「日付／カテゴリピル／タイトル」の1行91pxリスト4件＋下罫線、左下に黒ピルボタン。**4件**（行を増減できる） |
| news-002 | 白 | 見出しの下を左右2分割。左に主記事（サムネ＋日付＋32px/900の大見出し）、右に日付＋角タグ＋見出しの一覧5件。**5件**（行を増減できる） |
| news-003 | 白 | 左に英字70pxの見出し＋カテゴリナビ5項目（選択中はグレー面）、右に1行98pxの一覧3件（日付＋角タグ＋見出し＋矢印）と角型ボタン。**3件**（行を増減できる） |
| news-004 | 白 | 左にラベル＋英字62pxの見出し＋ピルボタン、右に1行58pxの一覧3件（日付・タイトル・右端に丸ドット）。**3件**（行を増減できる） |
| news-005 | 白 | 左にラベル＋見出し＋丸矢印リンク、右に1行84pxの一覧4件（日付＋角丸タグ＋見出し＋右端に矢印）。**4件**（行を増減できる） |
| news-006 | 白 | 左にサムネ192×108付きの一覧4件（カテゴリ＋16px lh26のタイトル）、右に英字72px/200の見出し＋一覧リンク。**4件**（行を増減できる） |
| news-007 | 白 | 左に英字64pxの見出し、右に「すべて見る」。下に1行122pxの一覧3件（日付＋タグ＋18pxタイトル＋右端に丸矢印40px）。**3件**（行を増減できる） |
| news-008 | 白 | 左に和文ラベル＋英字80pxの見出し、右上にリンク＋丸φ64。一覧3件（ピル110×30＋日付14px＋タイトル16px、行高121）。**3件**（行を増減できる） |
| news-009 | 明グレー | 左にカテゴリフィルタ（ピッチ54.5）、右にサムネ158×118付きの一覧3件（日付＋カテゴリ＋16pxタイトル）。**3件**（行を増減できる） |
| news-010 | 白 | 左に英字100pxの見出し＋View all、右に「日付＋見出し」の一覧3件（行高127・下罫線）。**3件**（行を増減できる） |
| news-011 | 白 | 左に正方形画像220角＋一覧ボタン、右に「日付／グレー面のカテゴリ／タイトル」の行リスト5件（行高73）。**5件**（行を増減できる） |
| news-012 | 白 | 英字50pxの見出し＋横タブ（すべて／カテゴリ3種）、下に2カラムの一覧6件（カテゴリピル＋日付＋タイトル）。**6件**（折り返しで増減できる） |
| news-013 | 白 | 和文42pxの見出し＋小見出し。日付を「日.月／年」の縦2段で見せる一覧4件（行高96・上下罫線）。**4件**（行を増減できる） |
| news-014 | 白 | 見出し32px＋左に縦積みの絞り込みタグ3つ、右に一覧4件（日付12px＋種別タグ11px＋タイトル16px・下罫線）。下に誘導ブロック624×96を2枚。**4件**（行を増減できる） |
### フッター `footer/`

| id | 背景 | 構造 |
|---|---|---|
| footer-001 | 白 | 上部に角丸バナー3カラム（各ラベル白ピル）。下は中央縦罫線で2分割し、左に住所・SNS・コピーライト、右にTEL・営業時間・黒の大ピルボタン。**3件** |
| footer-002 | 濃色 | 背面に巨大な英字マーキー（中央静止）、中央にリード、幅いっぱいの枠線ピルCTA（高さ190）、下端にリンク列＋SNSアイコン＋コピーライト。**3件**（行を増減できる） |
| footer-003 | 濃色 | 左にロゴ・住所、右にサイトマップ2列。右下に Page Top、1px罫線の下にSNSアイコン＋ポリシーリンク＋コピーライト。**3件** |
| footer-004 | 白（濃色帯） | 高さ262の濃色帯1本だけのシンプルフッター。ロゴ＋リンク列＋右端にコピーライト。**3件** |
| footer-005 | 白 | 左にロゴ・住所・角丸のDLボタン、右にサイトマップ3列。下部は罫線＋小リンク＋コピーライト。**2件**（行を増減できる） |
| footer-006 | 白 | 左にロゴ、右にリンク3行＋SNSアイコン縦並び。罫線の下にコピーライトとPage Topの丸ボタン。**2件**（行を増減できる） |
| footer-007 | 白 | 左にロゴ＋英字サブ、右にサイトマップ3列（列見出し14px＋リンク15px・行ピッチ44）。下部に11pxのポリシーリンク4本。**6件**（行を増減できる） |
| footer-008 | 濃色 | 左に社名・住所・丸型SNS、右に「英字48px/200＋和文」のリンクを2列4行。下部にポリシー＋コピーライト＋言語切替。**3件** |
| footer-009 | 白 | 左にロゴ＋コーポレートサイトリンク＋コピーライト、右に親子2階層のリンクを縦積み |
| footer-010 | 明グレー | 左にロゴ、中央にリンク列（16px/700・ピッチ36.5）。下にポリシー4本＋コピーライト＋PAGE TOP。**4件**（行を増減できる） |
| footer-011 | 白 | 左にロゴ＋タグライン18px、右にリンク2列（12px/600・ピッチ43）。下にポリシーとコピーライト。**5件**（行を増減できる） |
| footer-012 | 白 | 上部に採用バナー2枚（丸アイコン108＋見出し24px＋丸矢印）、下にロゴ＋親子リンク＋コピーライト。**2件** |
| footer-013 | 明グレー | 親子2階層のサイトマップを4カラムで並べ、下端にロゴとコピーライト。**4件** |
| footer-014 | 明グレー | 左に社名・住所（15px lh31.5）、右に「英字24px/300＋和文リンク」の列を4つ。**5件**（行を増減できる） |
| footer-015 | 白 | 店舗・医院の情報型。左にLOGO＋住所＋TEL/FAX＋営業時間表（8列3行）＋注記＋予約/電話ボタン、右に地図624×374。下端にコピーライト帯。**7件**（行を増減できる） |
## 新規セクションの作り方

### A. URL から作る（推奨・精度が高い）

1. ヘッドレスブラウザで **1440px 幅**でURLを開く
2. `document.querySelectorAll('section, header, footer')` を `getBoundingClientRect()` で走査し、**高さ200px以上・幅900px以上**のブロックをセクションとして抽出する（top順に並べる）
3. セクションごとに子要素を走査し、`rect`（x/y/w/h）と `getComputedStyle` の `fontSize / fontWeight / lineHeight / letterSpacing`、テキスト、画像かどうかを取り出す
4. 元サイトのコンテンツ枠（例 x90〜1350）を本テンプレートの **72〜1368** に線形変換する
5. **セクション高が800を超える場合は、余白を詰める・繰り返し要素の件数を減らして800に収める**（何を削ったかはCSSのコメントに残す）
6. 以降は下記5〜7と同じ

### B. スクリーンショットから作る

1. スクリーンショットを `site-wireframe/<カテゴリ名>/` に置く
2. 実寸を確認する（Retina の2倍解像度なら 1/2 が CSS px）
3. 各要素の座標・サイズ・フォントサイズを**ピクセル単位で実測**する
4. `<カテゴリ名>/<カテゴリ名>-<3桁連番>.html` を作る（カラム数で分類。`pagetitle` `mv` `news` `footer` のみ役割別）。下の「ベースCSS」を `<style>` の冒頭にコピーし、その下にセクション固有のCSSを `.ファイルID__要素名`（例 `.col2_012__head`）で書く（BEM風）
5. **1440×800 でレンダリングし、元スクショと数値を突き合わせて再検証**する。ズレていれば直して再検証
6. `index.html` の `DATA` 配列と、このファイルのカタログ表に1行追加する
7. 検証が通ったら、元スクショを削除する

### 許容誤差

- 座標・サイズは **±2px 以内**
- フォントの字形差（Poppins のスラッシュ幅、カタカナと漢字のインク差など）による見た目のズレは許容する。合わせるのは**送り幅と要素のボックス位置**であって、グリフのインク範囲ではない

### 再検証用コマンド

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu --hide-scrollbars --force-device-scale-factor=2 --window-size=1440,800 --screenshot=out.png --virtual-time-budget=6000 "file://<絶対パス>/mv/mv-001.html"
```

### ベースCSS（各HTMLの `<style>` 冒頭にコピー）

```css
:root {
  --wf-w: 1440px;
  --wf-h: 800px;
  --wf-content: 1296px;   /* コンテンツ幅 90% */
  --wf-pad: 72px;         /* 左右余白 */

  --c-black: #000;
  --c-text: #222;
  --c-sub: #555;
  --c-mute: #808080;
  --c-light: #b0b0b0;
  --c-line: #dcdcdc;
  --c-bg-gray: #f2f2f2;
  --c-img: #d9d9d9;
  --c-img-dark: #8c8c8c;
  --c-white: #fff;
}
* { margin: 0; padding: 0; box-sizing: border-box; }
html, body { height: 100%; }
body {
  background: #e9e9e9;
  font-family: "Noto Sans JP", sans-serif;
  font-weight: 400;
  color: var(--c-text);
  -webkit-font-smoothing: antialiased;
}
ul, ol { list-style: none; }
a { color: inherit; text-decoration: none; }

/* アートボード 1440×800。ビューポートに合わせて等倍スケールする */
.stage { position: fixed; inset: 0; overflow: hidden; }
.wf {
  position: relative;
  width: var(--wf-w); height: var(--wf-h);
  background: var(--c-white); overflow: hidden;
  left: 50%; top: 50%;
  transform: translate(-50%, -50%) scale(var(--s, 1));
  transform-origin: center;
  box-shadow: 0 4px 24px rgba(0,0,0,.18);
}
/* コンテンツ幅90%のインナー枠 */
.wf__inner { position: absolute; left: var(--wf-pad); width: var(--wf-content); }

.en { font-family: "Poppins", sans-serif; }
.img { position: relative; background: var(--c-img); }
.img--dark { background: var(--c-img-dark); }
.img[data-l]::after {   /* グレーボックスの右下に IMAGE / THUMB などのラベルを出す */
  content: attr(data-l);
  position: absolute; right: 10px; bottom: 8px;
  font-family: "Poppins", sans-serif; font-size: 9px; letter-spacing: .08em;
  color: #8a8a8a; line-height: 1;
}
.dot { font-size: 8px; line-height: 1; }
/* 行頭に全角スペース（U+3000）を残さないための2つ。左端を上下の要素と揃えるために使う
   .sep   … 区切りに全角スペースを使う代わりに、前の要素へ 1em 空ける（折り返しても行頭に空白が残らない）
   .flush … やむを得ず行頭に全角スペースがあるとき、その字下げを打ち消す */
.sep { margin-right: 1em; }
.flush { text-indent: -1em; }
.btn-black {
  display: inline-flex; align-items: center; justify-content: center;
  background: var(--c-black); color: var(--c-white);
  border-radius: 999px; white-space: nowrap;
}
.tag-white {   /* グレー面・画像の上に置くピル */
  display: inline-flex; align-items: center; justify-content: center;
  background: var(--c-white); color: var(--c-text);
  border-radius: 999px; white-space: nowrap;
}
.tag-gray {    /* 白背景の上に置くピル */
  display: inline-flex; align-items: center; justify-content: center;
  background: #e8e8e8; color: var(--c-text);
  border-radius: 999px; white-space: nowrap;
}
```

末尾のスケール用スクリプト：

```html
<script>
  const fit = () => document.documentElement.style.setProperty('--s', Math.min(innerWidth / 1440, innerHeight / 800));
  addEventListener('resize', fit); fit();
</script>
```
