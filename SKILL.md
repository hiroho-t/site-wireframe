---
name: site-wireframe
description: Webサイトのセクション単位のワイヤーフレームをHTMLで作るときに使う。MV・コンセプト・会社紹介・特長・レポート・会社情報・インタビュー・サービス・商品・パートナー・導入事例・メディア・お知らせ・採用・誘導バナー・お問い合わせ・フッターの24カテゴリ87型を1440×800のアートボードで持つ。「サイトのワイヤーを作って」「MVのレイアウト案がほしい」「お知らせ一覧の構成を」「フッターのワイヤーを」などの依頼で必ず使うこと。スクリーンショットを渡されてレイアウトを実測・トレースする場面でも使う。明示的に「スキル」と言われなくても、コーポレートサイト・サービスサイトのセクション構成をHTMLで組む場面では積極的に発火させる。
---

# サイトワイヤーフレーム テンプレート集

Webサイトのセクション型を収録している。実在のサイトのスクリーンショット、またはURLからレイアウトだけを採寸・再現し、テキストはすべてダミー化してある。**新規にゼロから組まず、まずこの中から近い型を選んで流用する。**

## 使い方

1. 下のカタログから、目的とテキスト量に合う型を選ぶ
2. 該当ファイル（例 `mv/mv-001.html`）を読む
3. コピーして、テキストと項目数だけ差し替える
4. 座標・フォントサイズ・配色は原則そのまま。要素が増減するときだけ、同じリズム（行間・余白の刻み）で調整する

新しいスクリーンショットやURLからトレースする場合は、末尾の「新規セクションの作り方」に従う。

## 共通仕様

- セクションは **1440×800 固定**。`.stage` の中の `.wf` に絶対配置し、JSでビューポートに合わせて等倍スケールする
- **CSSは共通ファイルにせず、各HTMLの `<style>` に直書きする**（1ファイル単体で完結させる）。外部読み込みは Google Fonts と Font Awesome の CDN のみ
- **コンテンツ幅は基本 90%**＝ `1440 × 0.9 = 1296px`、左右余白はそれぞれ `72px`。CSS変数 `--wf-content` / `--wf-pad` を使い、数値を直書きしない
- **背景は基本 白**。グレーや黒に変えるのは、そのままだと視認性に問題が出る場合だけ（例：全面画像の上に白抜き文字が乗るMV）
- 色は **白・黒・グレーのみ**
- 和文 **Noto Sans JP** ／ 欧文・数字 **Poppins**（`class="en"`）／ アイコン **Font Awesome 6 無料版**
- 写真・図版はグレーの矩形（`.img`）に置き換える。役割がある場合は `data-l="THUMB"` のようにラベルを付ける（右下に小さく出る）
- ロゴは `LOGO` テキスト（Poppins）。シンボルもグレーボックスも置かない
- 本文はダミーテキスト。**横並びで位置が連動する要素（ヘッダーナビ等）は元の文字数に合わせる**
- コピーライトは `© 20XX Company.` で統一
- 装飾（写真・グラデーション・影・ホバー・アニメーション）は再現しない。角丸と罫線だけ実測で合わせる
- **スライドショー・カルーセル・ティッカー・マーキーは動かさない。中央に静止させた状態でレイアウトだけを見せる**（見切れさせない）
- **サイト固有の変則レイアウトは、そのまま型にしない。** 型はあくまで「他の案件でも使い回せるオーソドックスな骨格」であること。
  次のようなものは**汎用形に正規化してから**収録する（正規化できないものは収録しない）。
  - 装飾目的のパララックス帯やタイルの敷き詰め → 素直な画像グリッドにする
  - 本文に画像を重ねる、円形に切り抜くなどの造形 → 矩形の2カラム or カード並びにする
  - 数値やカードが不揃いな位置に散る配置 → 等分グリッド＋罫線で揃える
  - そのサイトのコピー画像ありきで組まれたブロック → 見出し＋本文＋署名など、テキストで成立する構成に置き換える

### 縦位置のルール

- **MV とフッター以外のセクションは、アートボードの上下中央に配置する**
  （コンテンツの高さを出し、`top = (800 − コンテンツ高さ) ÷ 2`）
- **MV** は下端揃え（見出し・リード文はアートボード下辺基準）
- **フッター** は実測どおりの縦位置

### 実測から確定している基準サイズ

| 用途 | サイズ | 行間 |
|---|---|---|
| ヘッダーナビ | 12px / 500 | — |
| MV 大見出し | 56px / 700・letter-spacing 0.14em | 70px |
| MV リード文 | 12px | 24px |
| セクション見出し（和文） | 40px / 700 | 1 |
| セクション見出し（欧文 `/ Xxx`） | 24px / 600 | 1 |
| 一覧の日付・カテゴリ・タイトル | 14px | — |
| ボタン内テキスト（小 / 大） | 12px / 14px | — |
| 住所・SNS・コピーライト | 13px | 22.5px |
| 電話番号 | 36px / 500 | 1 |

### 実測済みの共通レイアウト値

| 項目 | 値 |
|---|---|
| コンテンツ枠 | left 72 / 幅 1296（右端 1368） |
| ヘッダー | top 14 / 高さ 60。ロゴ左寄せ、ナビ＋ハンバーガーを右寄せ（gap 30） |
| ヘッダー：白ピルナビ | 794×60 / radius 30、内側 padding-left 25、項目間 gap 32 |
| ヘッダー：CTA（黒） | ピル内 left 660 / top 12 / 117×36 |
| ヘッダー：ハンバーガー | 60×60 円（右端 1368 揃え） |
| 一覧1行（news） | 高さ 91 / 下罫線 1px。行内オフセット：日付 0・カテゴリ 106・タイトル 300 |
| バナー3カラム（footer） | 412×205 / gap 30 / radius 24 |

## カタログ

### 下層ページタイトル `pagetitle/`

| id | 背景 | 構造 |
|---|---|---|
| pagetitle-001 | 全面画像 | 下層ページのタイトル部。左上にパンくず12px、中段に英字96px/500＋和文28px＋リード16px |

### MV `mv/`

| id | 背景 | 構造 |
|---|---|---|
| mv-001 | 全面画像（グレー） | 左上ロゴ、右上に白ピルナビ＋黒CTA＋ハンバーガー。左下に大見出し2行、その下に縦罫線つきのリード文3行＋スライドカウンター |
| mv-002 | 白 | 高さ100のヘッダー（ロゴ左／ナビ＋黒ピルCTA右）。中央に64pxのキャッチコピー1行＋サブ、下端にアイコン付きティッカー（中央静止） |
| mv-003 | 明グレー | 高さ101・下罫線のヘッダー（ロゴ／ナビ／言語切替／角型CTA／ハンバーガー）。左に80pxの見出し2行＋サブ、右に大きな画像枠、下端に白のお知らせバー |
| mv-004 | 全面画像 | 透過ヘッダー（ロゴ／ナビ／言語／CTA）。右寄せの50pxコピー2行（行間81.5）のみのシンプルなMV |
| mv-005 | 全面画像 | ロゴ＋白ピルナビ（高さ58 r29）＋言語切替。中央に斜体76pxの英字コピー2行＋和文サブ、右端に縦のドットページャー |
| mv-006 | 濃色 | 左上に白いロゴブロック（260×122）、半透明のピルナビ＋白い言語ボタン。中央に和文28pxコピー＋ロゴ枠 |
| mv-007 | 全面動画 | 固定ヘッダー（ロゴ＋英字サブ／ナビ7項目14px／右に採用リンク）。左下にコピー画像782×60＋英字1行のミニマルなMV |
| mv-008 | 白 | 角丸でインセットした全面ビジュアル（1296幅）の上に、英字72px/200のラベル＋和文ラベル＋42pxコピー＋本文＋テキストリンク |
| mv-009 | 全面画像 | 高さ80のヘッダー（ロゴ＋サイト名／ナビ6項目／右にエントリー）。ビジュアル下端に幅1368のコピー画像 |
| mv-010 | 全面画像 | ナビ左寄せ＋右にMENUのヘッダー。右下に和文48px/700 lh55.5 の大コピー2行だけを置く |
| mv-011 | 全面画像 | 高さ80のヘッダー（ナビ8項目13px/600）。中央にロゴマーク枠、左下に和文56px/600の2行コピー、右下に英字ラベル＋小テキスト |

### コンセプト `concept/`

| id | 背景 | 構造 |
|---|---|---|
| concept-001 | 白 | 英字ラベル＋36pxのリード3行（強調部分だけグレー）。下段は左に小見出し、右に幅403の補足テキスト |
| concept-002 | 全面画像 | 左下寄せに英字58px/900の見出し＋リード2行だけを置く、写真主役のステートメント |
| concept-003 | 明グレー | 英字24pxラベル＋32px/500 lh56 のスローガン2行だけの1カラム。企業スローガンの見せ場に |

### メッセージ `message/`

| id | 背景 | 構造 |
|---|---|---|
| message-001 | 白 | 社長挨拶・理念メッセージの定番型。左に人物画像520×600、右に英字ラベル＋32pxの見出し＋本文＋署名（役職14px／氏名22px） |
| message-002 | 全面画像 | 英字64pxの見出し＋役職18px・氏名24px＋本文＋テキストボタン。代表メッセージの導入に |
| message-003 | 白（帯画像） | 高さ382の帯状ビジュアルの上に、左に和文40px/700の見出し、右に本文20px/700＋丸ボタンφ64 |

### 会社紹介 `about/`

| id | 背景 | 構造 |
|---|---|---|
| about-001 | 白 | ドット付きラベル＋英字70pxの見出し。下を2カラムに分け、左に36pxの和文リード2行、右に19px lh37.5 の長文3ブロック |
| about-002 | 明グレー | 左に32pxの見出し2行＋本文3行＋ピルボタン、右に3枚の画像を重ねたコラージュ |
| about-003 | 明グレー | 左に英字72px/200の大ラベル＋和文、右に28px/500 lh50.4 の大きめリード |
| about-004 | 明グレー | 全幅1カラム。英字24pxラベル＋32px/500 lh56の見出し2行＋16pxリード＋中央のテキストボタン |

### 特長 `feature/`

| id | 背景 | 構造 |
|---|---|---|
| feature-001 | 白 | 特長・強みの定番型。英字ラベル＋和文40pxの見出し＋リード、下に3カラムのカード（画像400×260＋番号＋見出し20px＋本文） |
| feature-002 | 白 | 画像630×300＋「見出し20px＋丸ボタン」＋本文の2カラム |
| feature-003 | 全面画像 | 見出しブロック（英字14→和文28→本文14×3行）だけを左に置き、右下にテキストリンク |
| feature-004 | 明グレー | 左に英字64pxの見出し＋下罫線のリンク行2本＋テキストボタン、右に画像480×452 |
| feature-005 | 白 | 左に英字64pxの見出し・右にリード、下に幅いっぱいの図版（1296×233）＋中央のテキストボタン |

### レポート `report/`

| id | 背景 | 構造 |
|---|---|---|
| report-001 | 白 | ラベル＋英字70pxの見出し＋18pxのリード2行。下に幅いっぱい・角丸24の大きなビジュアルカード |

### 数字で見る `data/`

| id | 背景 | 構造 |
|---|---|---|
| data-001 | 明グレー | 数字で見るの定番型。見出し＋リードの下に、縦罫線で等分した4カラム（ラベル16px＋数字60px＋単位20px＋注記13px） |
| data-002 | 白 | 見出し＋リードの下に、実績カード5枚を均等配置（ラベル16px＋数字56px＋単位16px＋注記12px） |

### 沿革 `history/`

| id | 背景 | 構造 |
|---|---|---|
| history-001 | 明グレー | 左に英字20pxラベル＋和文32pxの見出し、右にリード。下に幅いっぱい（1296×474）の年表図版 |

### 会社情報 `company/`

| id | 背景 | 構造 |
|---|---|---|
| company-001 | 白 | 左に英字70pxの見出し＋30pxの和文2行＋リード、その下に「サムネ70角＋和文＋英字＋矢印」の103pxリンク行3件。右に大きな画像枠 |
| company-002 | 白 | 左に英字44px/900の見出し2行、右に4カラムのカード（画像＋見出し＋右下に丸ボタン） |
| company-003 | 白 | 左に英字64pxの見出し＋下罫線のリンク行4本（ピッチ88）、右に画像508×478 |
| company-004 | 明グレー | 左に画像2枚をずらして重ね、右に下罫線のリンク行6本（20px/700・ピッチ70）＋右下にリンク＋丸φ64 |

### インタビュー `interview/`

| id | 背景 | 構造 |
|---|---|---|
| interview-001 | 白 | 上にアイキャッチ文＋英字90pxの大見出し＋リード。下に「写真477×330＋テキスト358」のカルーセル1件を中央に静止表示 |
| interview-002 | 白 | 見出し＋3カラムのカード（画像383×383の左上にタグ・左下に部署、下に見出し18px＋氏名） |

### サービス `service/`

| id | 背景 | 構造 |
|---|---|---|
| service-001 | 白 | 左上に英字90pxの見出し。右73%のカラムに「英字52px＋和文ラベル」の小見出しと、番号8%／タイトル41%／本文44%の行リスト3件（下罫線） |
| service-002 | 白 | ラベル＋英字80pxの見出し。下に「番号／画像574×330／右にテキスト＋ピルボタン」の横並び1件 |
| service-003 | 白 | 左に全面画像608×800＋白抜きの見出し、右に「番号（01/06）＋画像493×260＋見出し24px」のカードを縦積み |
| service-004 | 白 | 左に英字72px/200の見出し＋リンク、右に「画像280×210＋事業名36px＋サービス名＋本文」の行を3つ（ピッチ235） |
| service-005 | 白 | 左端に接地する縦長画像448×640、右に英字64px＋和文20px＋本文16px lh28＋テキストボタン |
| service-006 | 白 | 見出しの下に「番号＋見出し24px＋本文（幅788）＋右に画像359×202」の行を並べる |
| service-007 | 白 | 英字76px/700 lh68 の特大見出し2行＋2カラムのカード（画像620×280＋見出し26px＋丸アイコン＋本文）、右下にリンク |
| service-008 | 白 | 英字56pxの見出し＋右にリード。下に大カード638×480＋小カード317×238を4枚組み合わせたグリッド |

### 商品 `product/`

| id | 背景 | 構造 |
|---|---|---|
| product-001 | 白 | ラベル＋英字62pxの見出し、右にPrev/Next＋ピルボタン。下に商品カード（303幅・画像206）の横並び（中央静止） |

### お客様の声 `voice/`

| id | 背景 | 構造 |
|---|---|---|
| voice-001 | 白 | 左に英字56pxの見出し＋本文＋丸矢印つきMoreリンク、右に大きな画像730×486 |

### メディア `magazine/`

| id | 背景 | 構造 |
|---|---|---|
| magazine-001 | 白（グレー面） | グレーのパネルに英字40px/900の見出し＋リンク、下を1px罫線で区切った3カラムの記事グリッド（画像＋タイトル＋右下に日付） |
| magazine-002 | 白 | 見出し＋記事カード3枚（画像383×215＋角丸タグ＋日付＋見出し18px）。スライダーは中央静止 |

### 誘導バナー `banner/`

| id | 背景 | 構造 |
|---|---|---|
| banner-001 | 全面画像 | 画面を左右2分割した誘導バナー。各パネル左下にラベル＋英字46pxの見出し、右下に丸ボタンφ62 |
| banner-002 | 全面画像 | 左右2分割し、各パネル中央に英字64px/200＋和文14px/600だけを置く最小構成のバナー |
| banner-003 | 全面画像 | 全幅の大バナー（高さ262）を縦に2本。左に英字56px＋和文、右端に丸アイコン |
| banner-004 | 全面画像 | 2分割の誘導ブロック。英字36px/400＋和文16px/600 だけの最小構成 |

### ムービー `movie/`

| id | 背景 | 構造 |
|---|---|---|
| movie-001 | 白 | 左に見出しブロック（幅264）＋テキストリンク2本、右に縦長サムネ270×478を gap18 で3枚 |

### ギャラリー `gallery/`

| id | 背景 | 構造 |
|---|---|---|
| gallery-001 | 白 | 画像グリッドの定番型。英字ラベル＋和文40pxの見出しの下に、3列×2行のセル（画像416×230＋キャプション15px） |

### パートナー `partner/`

| id | 背景 | 構造 |
|---|---|---|
| partner-001 | 白 | 上に英字90pxの見出し、下に右寄せの大きな画像枠（ロゴ一覧などを想定） |

### 導入事例 `case/`

| id | 背景 | 構造 |
|---|---|---|
| case-001 | 白 | コンパクトな見出し＋3カラムのカード（サムネ／タグ／タイトル／社名・日付）。3枚とも開始位置を揃える |
| case-002 | 白 | 縦長カード3枚（画像290×435）。画像上に番号・地域・日付を重ね、下部にタイトル18px＋英字 |

### 採用 `career/`

| id | 背景 | 構造 |
|---|---|---|
| career-001 | 白 | ラベル＋英字70pxの見出し＋36pxの和文2行、右上に角型ボタン。下に写真4枚のスライダー（中央静止） |
| career-002 | 白（濃色パネル） | 全幅のカードを左右分割。左の濃色パネルにラベル＋見出し2行＋本文＋白ボタン、右に画像 |
| career-003 | 白 | 左に見出しブロック（幅226）、右に4カード（H3 18px→英字12px→画像186×135）を gap62.5 で横並び |
| career-004 | 明グレー | 左に英字72px/200＋42pxコピー＋本文＋リンク、右に255×307のタイルを2列×2行のグリッドで（元は流れるタイル→静止グリッドに正規化） |
| career-005 | 白 | 背景に縦長タイル5枚（250幅・gap69）を敷き、中央にラベル＋英字80px＋本文＋丸ボタン |

### お問い合わせ `contact/`

| id | 背景 | 構造 |
|---|---|---|
| contact-001 | 白（濃色パネル） | 幅いっぱい・角丸16の濃色パネル。左にラベル＋英字70pxの見出し＋リード2行、右に白い角型ボタン |
| contact-002 | 全面画像 | 英字74pxの見出し＋リード3行＋半透明の箇条書きパネル（580幅） |
| contact-003 | 全面画像 | 中央揃えで英字88px/500の特大コピー＋24pxリード＋テキストボタン。採用エントリー導線の締めに使う |
| contact-004 | 全面画像 | 英字56pxの見出し＋本文16px/600（幅667）、右に枠線のボタン276×76 |

### お知らせ `news/`

| id | 背景 | 構造 |
|---|---|---|
| news-001 | 白 | 左上に和文大見出し＋英字サブ。下に「日付／カテゴリピル／タイトル」の1行91pxリスト4件＋下罫線、左下に黒ピルボタン |
| news-002 | 白 | 見出しの下を左右2分割。左に主記事（サムネ＋日付＋32px/900の大見出し）、右に日付＋角タグ＋見出しの一覧5件 |
| news-003 | 白 | 左に英字70pxの見出し＋カテゴリナビ5項目（選択中はグレー面）、右に1行98pxの一覧3件（日付＋角タグ＋見出し＋矢印）と角型ボタン |
| news-004 | 白 | 左にラベル＋英字62pxの見出し＋ピルボタン、右に1行58pxの一覧3件（日付・タイトル・右端に丸ドット） |
| news-005 | 白 | 左にラベル＋見出し＋丸矢印リンク、右に1行84pxの一覧4件（日付＋角丸タグ＋見出し＋右端に矢印） |
| news-006 | 白 | 左にサムネ192×108付きの一覧4件（カテゴリ＋16px lh26のタイトル）、右に英字72px/200の見出し＋一覧リンク |
| news-007 | 白 | 左に英字64pxの見出し、右に「すべて見る」。下に1行122pxの一覧3件（日付＋タグ＋18pxタイトル＋右端に丸矢印40px） |
| news-008 | 白 | 左に和文ラベル＋英字80pxの見出し、右上にリンク＋丸φ64。一覧3件（ピル110×30＋日付14px＋タイトル16px、行高121） |
| news-009 | 明グレー | 左にカテゴリフィルタ（ピッチ54.5）、右にサムネ158×118付きの一覧3件（日付＋カテゴリ＋16pxタイトル） |

### フッター `footer/`

| id | 背景 | 構造 |
|---|---|---|
| footer-001 | 白 | 上部に角丸バナー3カラム（各ラベル白ピル）。下は中央縦罫線で2分割し、左に住所・SNS・コピーライト、右にTEL・営業時間・黒の大ピルボタン |
| footer-002 | 濃色 | 背面に巨大な英字マーキー（中央静止）、中央にリード、幅いっぱいの枠線ピルCTA（高さ190）、下端にリンク列＋SNSアイコン＋コピーライト |
| footer-003 | 濃色 | 左にロゴ・住所・認証マーク、右にサイトマップ2列。右下に Page Top、1px罫線の下にSNSアイコン＋ポリシーリンク＋コピーライト |
| footer-004 | 白（濃色帯） | 高さ262の濃色帯1本だけのシンプルフッター。ロゴ＋リンク列＋右端にコピーライト |
| footer-005 | 白 | 左にロゴ・住所・角丸のDLボタン、右にサイトマップ3列。下部は罫線＋小リンク＋コピーライト |
| footer-006 | 白 | 左にロゴ、右にリンク3行＋SNSアイコン縦並び。罫線の下にコピーライトとPage Topの丸ボタン |
| footer-007 | 白 | 左にロゴ＋英字サブ、右にサイトマップ3列（列見出し14px＋リンク15px・行ピッチ44）。下部に11pxのポリシーリンク4本 |
| footer-008 | 濃色 | 左に社名・住所・丸型SNS、右に「英字48px/200＋和文」のリンクを2列4行。下部にポリシー＋コピーライト＋言語切替 |
| footer-009 | 白 | 左にロゴ画像＋コーポレートサイトリンク＋コピーライト、右に親子2階層のリンクを縦積み |
| footer-010 | 明グレー | 左にロゴ、中央にリンク列（16px/700・ピッチ36.5）、右に認証マーク枠。下にポリシー4本＋コピーライト＋PAGE TOP |
| footer-011 | 白 | 左にロゴ＋タグライン18px、右にリンク2列（12px/600・ピッチ43）。下にポリシーとコピーライト |

## 新規セクションの作り方

### A. URL から作る（推奨・精度が高い）

1. ヘッドレスブラウザで **1440px 幅**でURLを開く
2. `document.querySelectorAll('section, header, footer')` を `getBoundingClientRect()` で走査し、**高さ200px以上・幅900px以上**のブロックをセクションとして抽出する（top順に並べる）
3. セクションごとに子要素を走査し、`rect`（x/y/w/h）と `getComputedStyle` の `fontSize / fontWeight / lineHeight / letterSpacing`、テキスト、画像かどうかを取り出す
4. 元サイトのコンテンツ枠（例 x90〜1350）を本テンプレートの **72〜1368** に線形変換する
5. **セクション高が800を超える場合は、余白を詰める・繰り返し要素の件数を減らして800に収める**（何を削ったかはCSSのコメントに残す）
6. 以降は下記5〜7と同じ

### B. スクリーンショットから作る

1. スクリーンショットを `site-wireframe/<カテゴリ名>/` に置く
2. 実寸を確認する（Retina の2倍解像度なら 1/2 が CSS px）
3. 各要素の座標・サイズ・フォントサイズを**ピクセル単位で実測**する
4. `<カテゴリ名>/<カテゴリ名>-<3桁連番>.html` を作る。下の「ベースCSS」を `<style>` の冒頭にコピーし、その下にセクション固有のCSSを `.カテゴリ名__要素名` で書く（BEM風）
5. **1440×800 でレンダリングし、元スクショと数値を突き合わせて再検証**する。ズレていれば直して再検証
6. `index.html` の `DATA` 配列と、このファイルのカタログ表に1行追加する
7. 検証が通ったら、元スクショを削除する

### 許容誤差

- 座標・サイズは **±2px 以内**
- フォントの字形差（Poppins のスラッシュ幅、カタカナと漢字のインク差など）による見た目のズレは許容する。合わせるのは**送り幅と要素のボックス位置**であって、グリフのインク範囲ではない

### 再検証用コマンド

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" --headless --disable-gpu --hide-scrollbars --force-device-scale-factor=2 --window-size=1440,800 --screenshot=out.png --virtual-time-budget=6000 "file://<絶対パス>/mv/mv-001.html"
```

### ベースCSS（各HTMLの `<style>` 冒頭にコピー）

```css
:root {
  --wf-w: 1440px;
  --wf-h: 800px;
  --wf-content: 1296px;   /* コンテンツ幅 90% */
  --wf-pad: 72px;         /* 左右余白 */

  --c-black: #000;
  --c-text: #222;
  --c-sub: #555;
  --c-mute: #808080;
  --c-light: #b0b0b0;
  --c-line: #dcdcdc;
  --c-bg-gray: #f2f2f2;
  --c-img: #d9d9d9;
  --c-img-dark: #8c8c8c;
  --c-white: #fff;
}
* { margin: 0; padding: 0; box-sizing: border-box; }
html, body { height: 100%; }
body {
  background: #e9e9e9;
  font-family: "Noto Sans JP", sans-serif;
  font-weight: 400;
  color: var(--c-text);
  -webkit-font-smoothing: antialiased;
}
ul, ol { list-style: none; }
a { color: inherit; text-decoration: none; }

/* アートボード 1440×800。ビューポートに合わせて等倍スケールする */
.stage { position: fixed; inset: 0; overflow: hidden; }
.wf {
  position: relative;
  width: var(--wf-w); height: var(--wf-h);
  background: var(--c-white); overflow: hidden;
  left: 50%; top: 50%;
  transform: translate(-50%, -50%) scale(var(--s, 1));
  transform-origin: center;
  box-shadow: 0 4px 24px rgba(0,0,0,.18);
}
/* コンテンツ幅90%のインナー枠 */
.wf__inner { position: absolute; left: var(--wf-pad); width: var(--wf-content); }

.en { font-family: "Poppins", sans-serif; }
.img { position: relative; background: var(--c-img); }
.img--dark { background: var(--c-img-dark); }
.img[data-l]::after {   /* グレーボックスの右下に IMAGE / THUMB などのラベルを出す */
  content: attr(data-l);
  position: absolute; right: 10px; bottom: 8px;
  font-family: "Poppins", sans-serif; font-size: 9px; letter-spacing: .08em;
  color: #8a8a8a; line-height: 1;
}
.dot { font-size: 8px; line-height: 1; }
.btn-black {
  display: inline-flex; align-items: center; justify-content: center;
  background: var(--c-black); color: var(--c-white);
  border-radius: 999px; white-space: nowrap;
}
.tag-white {   /* グレー面・画像の上に置くピル */
  display: inline-flex; align-items: center; justify-content: center;
  background: var(--c-white); color: var(--c-text);
  border-radius: 999px; white-space: nowrap;
}
.tag-gray {    /* 白背景の上に置くピル */
  display: inline-flex; align-items: center; justify-content: center;
  background: #e8e8e8; color: var(--c-text);
  border-radius: 999px; white-space: nowrap;
}
```

末尾のスケール用スクリプト：

```html
<script>
  const fit = () => document.documentElement.style.setProperty('--s', Math.min(innerWidth / 1440, innerHeight / 800));
  addEventListener('resize', fit); fit();
</script>
```
